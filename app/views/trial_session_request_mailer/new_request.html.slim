h1 New request for a trial session!

p #{@trial_session_request.name} from #{@trial_session_request.company} has requested a trial session.

p The client specified the following availability:

blockquote
  pre = @trial_session_request.availability

p Please #{link_to 'act upon the request', trial_session_request_url(@trial_session_request, locale: :de)} soon!
